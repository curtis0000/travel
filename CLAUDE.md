# CLAUDE.md — travel（把 md 行程變成可分享的手機行程網頁）

## 專案是什麼

旅程主揪用 Markdown 寫行程（哪裡停多久、怎麼移動、預估幾分鐘），
這個網頁把 `trips/*.md` 即時轉成手機優先的互動頁面，一個網址就能分享給同行朋友。
目標使用者是旅途中拿手機看的人：看今天的時間軸、按一下開地圖導航、勾待辦。

Markdown 是唯一的內容來源。以後新增行程 = 丟一個 md 進 `trips/` + 在 `trips/index.json` 加一行。

## 技術棧

- 純前端單檔 `index.html`（HTML + CSS + JS 全在裡面），無 build 工具、無框架
- Markdown 解析：`marked`（jsDelivr CDN）；行程結構（天數、時間軸）由自己的 parser 從 md 文字抽出
- 部署：GitHub Pages，repo `curtis0000/travel`（公開），網址 `https://curtis0000.github.io/travel/?trip=<id>`
- 本機預覽：`python3 -m http.server 9100` 後開 `http://localhost:9100/`（fetch md 需要 http，直接開檔案不行；8000 被別的專案占用）

## 核心原則（不可違反）

1. **沒有後端、沒有登入、沒有資料庫。** 內容只住在 md，改行程就改 md 重新上傳。這是刻意的：主揪非工程背景，維護成本要趨近零。
2. **色塊不截斷文字。** 時間軸的停留色塊高度由內容決定，文字多就長高。絕不用 `overflow: hidden`、`line-clamp` 或固定高度把行程文字切掉。（使用者原話：「不要為了等高就把文字切掉」）
3. **手機優先。** 先在 390px 寬設計，再放大到桌機。表格在手機上改成卡片，不做橫向捲動。
4. **md 寫法不強迫。** parser 認得的格式（見下）是加分，認不得的內容一律退回一般 Markdown 顯示，絕不吞掉或報錯。
5. **勾選狀態只存在瀏覽器（localStorage）**，每個人各自的，不同步。要同步就需要後端，違反第 1 條。
6. 一律繁體中文 UI 文案。

## md 行程格式（parser 認得的規則）

```markdown
# 行程標題                          ← H1 = 頁首標題；結尾括號內的日期範圍會抽出來另外顯示
## 行程總覽                          ← 這個 H2 變成第一個分頁「總覽」
## 每日行程                          ← 這個 H2 底下的 H3 才算「天」
### 12/31（四）                      ← H3 = 一天，變成天數分頁
- 16:35–17:35 抵達成田機場：入境＋領行李   ← 「HH:MM–HH:MM 地點：備註」= 停留色塊
  → 京成 Skyliner 到日暮里 40 分         ← 「→」開頭 = 移動連接條，抓「N 分」「N 小時」算時長
- 19:00 池袋入住                      ← 只有單一時間 = 沒有長度的節點
- 注意：地鐵跨年不跑通宵                ← 「注意：」開頭 = 警示框
- 沒有時間的項目                       ← 保留為一般條列，排在該天時間軸下方
- [ ] 待辦                            ← 任何地方的 checkbox 都可勾，存 localStorage
```

- 時間分隔接受 `–`（en dash）和 `-`。結束時間比開始小（如 22:30–01:00）視為跨午夜。
- **地點名 = 時間之後、第一個「：」之前的文字**。地圖按鈕直接拿這段去 Google Maps 搜尋，不要求使用者在 md 額外標記。
- 時間衝突判斷：上一站結束 + 移動時長 > 下一站開始 → 標紅「時間不夠」；有空檔 → 顯示「自由時間 N 分」。

## 版面規格

- 頁首：標題、日期範圍、分享按鈕（手機叫出系統分享，桌機複製網址）
- 分頁列（sticky 頂部）：「總覽」+ 每一天。旅程進行中自動選到今天，否則選總覽。網址 `#o`、`#d0`… 可直接分享指定分頁
- 每天：時間軸（停留色塊 + 移動連接條）→ 沒有時間的條列 → 該天結束時間
- 停留色塊：地點名、時長、地圖按鈕（開 Google Maps 搜尋地點名）、有備註才有展開箭頭
- 「每日行程」以外的所有 H2 段落（住宿、購物清單、餐廳、待辦）全部收進「總覽」分頁，接在行程總覽後面照一般 Markdown 顯示。每日分頁只有時間軸，不重複放這些
- 表格在 ≤600px 改成卡片（欄名 → 每列的標籤）
- 「地點」分頁（最後一個）：列出時間軸所有停留地點，照點選順序編號，按鈕用 Google Maps Directions URL 開路線（origin／waypoints／destination，最多 10 點，可選步行／大眾運輸／開車）。勾選不存檔，離開頁面就清空
- 固定淺色（使用者決定，戶外看得清楚），不做深色模式；列印時展開所有天
- 視覺方向「雪影藍路線圖」：純白底（#fff，絕不加米色／奶茶色調）、單一靛藍品牌色（雪地影子的顏色）、粗黑體標題（Noto Sans TC 900）、數字用 Bricolage Grotesque。時間軸畫成地鐵路線圖：粗線是路線、白圈是站
- 一律禁止（使用者審美疲勞）：米色／奶茶底、襯線大標＋等寬小標的「編輯風」、細線分隔＋大量留白、字距拉開的全大寫小標籤（DAY 01、NOTE 那種）、卡片左側粗色條

## 範圍邊界

第一版只做上面「版面規格」列的東西。明確不做：多人共編、即時同步、離線快取（PWA）、密碼保護、地圖嵌入（只開外部連結）。

## 開發慣例

- 不自動 commit / push；使用者確認後才由使用者操作或指示執行
- 進度寫在 `PROGRESS.md`、決策與坑寫在 `docs/DEVLOG.md`，照 `progress-log` skill
- 原始未轉換的行程檔留在 `docs/原始行程_*.md` 供對照，不被網頁讀取

## 開發工作流：一步驟一對話

本專案採「一步驟一對話」開發，配合清對話節省 context：

1. 每次開新對話，固定先讀兩個事實來源：CLAUDE.md（規則與格式，不可違反的鐵則）+ PROGRESS.md（目前做到哪、接下來做什麼）。有需要才翻 docs/DEVLOG.md 查「當時為什麼這樣做」。
2. 動手前先用幾句話複述本步驟要做什麼，等開發者確認再開始。
3. 做完後告知如何驗收（本機預覽網址、要看哪一天），並更新 PROGRESS.md。
4. 開發者驗收 → 清對話 → 再開新對話做下一步。

PROGRESS.md 維護：保持精簡、只朝前看。膨脹時不要自行刪減，先請開發者過目再歸檔。

## 第二階段預留（現在不做，設計別擋路）

- 多份行程之間的共用資訊（例如飯店地址）：現在每份 md 各寫各的即可
- PWA 離線快取：`index.html` 保持單檔、md 用相對路徑 fetch，之後加 service worker 不用改結構
- 地點標記 `[[地點]]`：若自動抓地點常搜錯再加，parser 預留「先找 [[ ]]，沒有再用冒號前文字」的順序
